# Build artifacts
.next/
*.tsbuildinfo

# Dependencies
node_modules/

# Local environment (NEVER commit)
.env.local
.env*.local

# Operational state (NEVER commit)
.paper/
paper-trail-backup-*/
shadow-closed-trades*.json
reports/

# Editor/IDE
.cursor/
.DS_Store
